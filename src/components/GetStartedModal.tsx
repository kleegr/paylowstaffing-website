// Re-export for backward compatibility. The actual implementation moved to
// SignUpModal.tsx (iframe-based sign-up). Any pages/imports referencing the
// old GetStartedModal will keep working.
export { default } from './SignUpModal';
